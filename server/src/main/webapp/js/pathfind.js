
var showHelp = true;

function initiatePathFindingProcess() {
    globalMap.enableLayer(LayerNames.LocationSuggestion);
    globalMap.centerOnLayer(LayerNames.LocationSuggestion);
    // TODO Show message explaining the points and to select one
}
